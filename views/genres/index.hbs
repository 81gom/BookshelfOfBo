<h1>{{title}}</h1>
<a href="/genres/add">Add a new genre</a>
<table class="table table-striped table-hover">
    <thead>
        <th>Genre name</th>
    </thead>
    <tbody>
        {{#each dataset}}
            <tr>
                <td>{{this.name}}</td>
            </tr>
        {{/each}}
    </tbody>
</table>